interface Usuario {
  email: string;
  username: string;
  name: string;
  profile_picture_url: string;
}
interface Produto {
  id: number;
  nome: string;
}
interface Loja {
  id: number;
  nome: string;
}
interface Avaliacao {
  id: number;
  titulo: string;
}